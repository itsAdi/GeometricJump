#if UNITY_ENGINE
using UnityEngine;

public class CompareWithCurve : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
#endif
